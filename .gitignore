# Local runtime config (contains bot token / chat id — never commit)
config.json
/etc/ssh-alertd/

# Compiled binary / build output
/ssh-alertd
ssh-alertd
*.exe
/dist/
/build/

# Go test & coverage artifacts
*.test
*.out
coverage.*

# makepkg build artifacts
pkg/
src/
*.pkg.tar.*
*.src.tar.*

# Editor / OS junk
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db
